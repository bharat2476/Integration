# Integration

New product workspace — local folder `Integrations`, GitHub repo [bharat2476/Integration](https://github.com/bharat2476/Integration).

## Local setup

```powershell
cd C:\Users\agarw\Integrations
```

## Push your first commit

After you add application code:

```powershell
git add .
git commit -m "Initial product scaffold"
git push -u origin main
```

If the default branch on GitHub is `master` instead of `main`, use `git push -u origin master` or rename the branch to match GitHub.
